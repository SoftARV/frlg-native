build/
out/
*.gba
*.elf
*.map
*.sym
*.sav
.cache/
compile_commands.json
CMakeUserPresets.json
CLAUDE.local.md
.DS_Store
shots/
*.ppm

# Golden images: frames of the player ROM, generated per machine (ADR 0010).
tests/golden/images/
__pycache__/

# The launcher's own build tree (Meson, not CMake).
launcher/build/

# flatpak build artifacts
ports/desktop/flatpak/repo/
ports/desktop/flatpak/build-dir/
ports/desktop/flatpak/.flatpak-builder/

# Written by tools/package_flatpak.sh so a build with no .git can name its commit.
launcher/build-id.txt
